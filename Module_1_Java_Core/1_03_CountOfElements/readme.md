# Count of elements
## Task
The task was to implement a method which would find out
the frequency of each element of an array and put found
frequencies into a map.

## Launch
You would need to have **JDK 17** and **Apache Maven**.

Use the following commands:
```shell
mvn compile
mvn exec:java
```