# Complex Task Executor
## Task
The goal was to make use of `ExecutorService` and `CyclicBarrier` to "solve a task".

## Launch
You would need to have **JDK 17** and **Apache Maven**.

Use the following commands:
```shell
mvn compile
mvn exec:java
```