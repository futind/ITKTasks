# Blocking Queue
## Task
The goal was to implement a simple blocking queue with `enqueue(T item)`, `dequeue()` and `size()` 
methods using `wait()`/`notify`.

## Launch
You would need to have **JDK 17** and **Apache Maven**.

Use the following commands:
```shell
mvn compile
mvn exec:java
```