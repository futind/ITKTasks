# Simple Spring Security Config
## Task
The task was to create a simple Spring app with
**Spring Security** and to create a security configuration
which would prohibit unauthorized access to all the resources
but one.

## Launch
To run the application use:
```shell
./gradlew bootRun
```